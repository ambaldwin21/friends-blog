<div class="container">
  {{#each users}}
  <div>
    <h4><a href="/users/{{this.id}}">{{this.first_name}} {{this.last_name}}</a></h4>
    <img class="user-image circle" src="{{this.image}}"/>
  </div>
  {{/each}}
</div>
